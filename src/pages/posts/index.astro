---
import Layout from "../../layouts/Layout.astro";
import { Icon } from "astro-icon/components";

const POSTS_PER_PAGE = 10;

const allPosts = Object.values(
  import.meta.glob("./*.md", { eager: true }),
).sort(
  (a: any, b: any) =>
    new Date(b.frontmatter.pubDate).getTime() -
    new Date(a.frontmatter.pubDate).getTime(),
);

const posts = allPosts.slice(0, POSTS_PER_PAGE);
const totalPages = Math.ceil(allPosts.length / POSTS_PER_PAGE);
---

<Layout>
  <div class="container">
    <h2>医学相关文章</h2>

    <p>mcenahle.care 医学相关文章</p>

    <article class="ucla-card ucla-card__story">
      {
        posts.map((post: any) => (
          <div class="ucla-card__body">
            <p class="ucla-card__date">{post.frontmatter.pubDate}</p>
            <h3 class="ucla-card__title">
              <a
                class="ucla-card__title-link"
                href={post.url}
                title={post.frontmatter.title}
              >
                {post.frontmatter.title}
              </a>
            </h3>
            <p class="ucla-card__author">By {post.frontmatter.author}</p>
            <p class="ucla-card__description">{post.frontmatter.description}</p>
          </div>
        ))
      }
    </article>

    <!-- {
      totalPages > 1 && (
        <nav class="ucla-pagination" aria-label="分页">
          <ul class="ucla-pagination--list">
            <li class="ucla-pagination--list-item" aria-current="page">
              <a
                class="ucla-pagination--page ucla-pagination--page--current"
                href="/posts/page/1/"
                aria-label="第 1 页"
              >
                1
              </a>
            </li>

            <li class="ucla-pagination--list-item">
              <a
                class="ucla-pagination--page"
                href="/posts/page/2/"
                aria-label="第 2 页"
              >
                2
              </a>
            </li>

            {totalPages > 2 && (
              <li class="ucla-pagination--list-item">
                <a class="ucla-pagination--page">...</a>
              </li>
            )}

            {totalPages > 2 && (
              <li class="ucla-pagination--list-item">
                <a
                  class="ucla-pagination--page"
                  href={`/posts/page/${totalPages}/`}
                  aria-label={`第 ${totalPages} 页`}
                >
                  {totalPages}
                </a>
              </li>
            )}

            <li class="ucla-pagination--list-item">
              <a
                class="ucla-pagination--page"
                href="/posts/page/2/"
                aria-label="下一页"
              >
                下一页
              </a>
            </li>
          </ul>
        </nav>
      )
    } -->
  </div>
</Layout>
